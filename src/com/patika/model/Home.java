package com.patika.model;

import java.math.BigDecimal;

public class Home extends Residence {

    public Home(BigDecimal price, BigDecimal squareMeter, Integer numberOfLivingRoom, Integer numberOfRoom) {
        super(price, squareMeter, numberOfLivingRoom, numberOfRoom);
    }

}
